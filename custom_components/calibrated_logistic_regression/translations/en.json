{
  "config": {
    "step": {
      "user": {
        "title": "Calibrated Logistic Regression",
        "description": "Create a probability sensor from feature entities and model coefficients.",
        "data": {
          "name": "Name",
          "intercept": "Intercept",
          "coefficients": "Coefficients JSON",
          "required_features": "Required features",
          "state_mappings": "State mappings JSON",
          "calibration_slope": "Calibration slope",
          "calibration_intercept": "Calibration intercept"
        }
      }
    },
    "error": {
      "invalid_coefficients": "Coefficients must be valid JSON object of entity_id to number.",
      "invalid_state_mappings": "State mappings must be JSON object of entity_id to state-to-number mapping.",
      "required": "This field is required."
    },
    "abort": {
      "already_configured": "A sensor with this name is already configured."
    }
  },
  "options": {
    "step": {
      "init": {
        "title": "Calibration Options",
        "data": {
          "calibration_slope": "Calibration slope",
          "calibration_intercept": "Calibration intercept",
          "state_mappings": "State mappings JSON"
        }
      }
    }
  }
}
